---
title: Profiling
description: Reference for RiftRelay's optional Go pprof endpoints.
icon: Activity
---

Set `ENABLE_PPROF=true` to mount Go's standard pprof handlers under `/debug/pprof/`. Disabled by default — only turn it on when you need performance diagnostics.

## Endpoints

- `GET /debug/pprof/` — index
- `GET /debug/pprof/profile` — CPU profile
- `GET /debug/pprof/trace` — execution trace
- `GET /debug/pprof/cmdline`, `GET /debug/pprof/symbol` — tooling support

None of these are registered when `ENABLE_PPROF=false`.

## Usage

Point `go tool pprof` or `go tool trace` at `http://localhost:8985`. See the [Go pprof docs](https://pkg.go.dev/runtime/pprof) for the full reference.
